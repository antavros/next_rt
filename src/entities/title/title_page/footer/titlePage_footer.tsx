import Image from 'next/image'

import "@/entities/title/rate/rate.css";

export function TitlePageFooter({ details }: { readonly details: any }) {
    return (
        <div className='titlePage_footer'>
            
        </div>
    );
}